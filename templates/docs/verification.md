# Verification

Run:

```bash
./init.sh
```

Profile: `{{profile}}`
Workflow: `{{workflow}}`

Closure for TDD/SDD requires a completed audit from `docs/audit.md` with `GO`, `GO-WITH-RISK`, or `NO-GO`.
